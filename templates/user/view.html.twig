{% extends 'admin/_layout.html.twig' %}
{% set active_section = 'users' %}

{% block title %}User Details | Evec Tours{% endblock %}

{% block admin_body %}
    <section>
        <h1>User Details</h1>

        <div class="card user-card">
            <div class="card-body">
                <p><strong>ID:</strong> {{ user.id }}</p>
                <p><strong>Username:</strong> {{ user.username }}</p>
                <p><strong>Email:</strong> {{ user.email }}</p>
                <p><strong>Phone:</strong> {{ user.tel ?: '—' }}</p>
                <p><strong>Created At:</strong> {{ user.created_at ?: '—' }}</p>
                {% if user.image_url %}
                    <p><strong>Profile Image:</strong></p>
                    <img src="{{ user.image_url }}" alt="{{ user.username }}" class="user-avatar"/>
                {% endif %}
            </div>
            <div class="card-actions">
                <a class="btn btn-small" href="{{ path('admin_users_edit', {'id': user.id}) }}">Edit</a>
                <a class="btn btn-small" href="{{ path('admin_users') }}">Back</a>
            </div>
        </div>
    </section>
{% endblock %}
