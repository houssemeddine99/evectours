framework:
    cache:
        pools:
            cache.api_external:
                adapter: cache.adapter.filesystem
                default_lifetime: 3600      # 1 hour — weather + country data

            cache.ai_responses:
                adapter: cache.adapter.filesystem
                default_lifetime: 172800    # 48 hours — LLM output
